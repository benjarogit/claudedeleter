name: Build extensions

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build all targets
        run: npm run build

      - name: Verify artifacts
        run: |
          test -f dist/acc-chrome.zip
          test -f dist/acc-firefox.zip
          test -f dist/acc-firefox.xpi
          test -f dist/acc-edge.zip
          test -f dist/acc-console.js
          test -f dist/bookmarklet.txt
          unzip -l dist/acc-firefox.zip | grep manifest.json
          unzip -p dist/acc-firefox.zip manifest.json | grep -q '"scripts"'
          unzip -p dist/acc-firefox.zip manifest.json | grep -q 'data_collection_permissions'

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: acc-build
          path: |
            dist/*.zip
            dist/*.xpi
            dist/acc-console.js
            dist/bookmarklet.txt
            dist/*README.txt
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: acc-build
          path: dist

      - name: Attach to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/acc-chrome.zip
            dist/acc-firefox.zip
            dist/acc-firefox.xpi
            dist/acc-edge.zip
            dist/acc-console.js
            dist/bookmarklet.txt
            dist/CONSOLE-README.txt
            dist/AMO-README.txt
            dist/MOBILE-README.txt
            dist/SAFARI-README.txt
          overwrite_files: true
